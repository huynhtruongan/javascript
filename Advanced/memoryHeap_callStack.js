// REF: memory heap
    /*
       - is a free store, is a large region in memory that the engine provide 
       for us
    */
// REF: callStack
    /*
       - is used tp store function and variable as your code executes
       at each entry State of the stack and call these stack over and over
       unstill the command done then remove all stacks in callStack
    */
// REF: javascript is garbage collection
// REF: memorty leak
   // global variable
   // event listeners
   // setInerval